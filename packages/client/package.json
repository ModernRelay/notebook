{
  "name": "@omnigraph/client",
  "version": "0.0.1",
  "private": true,
  "description": "Typed HTTP client for omnigraph-server + ServerSource adapter that satisfies @omnigraph/executor's Source interface (translates fixture-DSL queries and MutationSpec to .gq).",
  "type": "module",
  "sideEffects": false,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@omnigraph/executor": "workspace:*",
    "@omnigraph/notebook-spec": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.5"
  }
}
